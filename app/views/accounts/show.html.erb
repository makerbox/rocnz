<div class="row">
<div class="col-md-6">
<p>
  <strong>name:</strong>
  <%= @account.company %>
</p>

<p>
  <strong>street:</strong>
  <%= @account.street %>
</p>

<p>
  <strong>Suburb:</strong>
  <%= @account.suburb %>
</p>

<p>
  <strong>State:</strong>
  <%= @account.state %>
</p>

<p>
  <strong>Country:</strong>
  <%= @account.country %>
</p>

<p>
  <strong>Phone:</strong>
  <%= @account.phone %>
</p>

<p>
  <strong>Contact name:</strong>
  <%= @account.contact %>
</p>


<% if !@account.approved? %>
<p>
Your account is pending approval.
</p>
<% end %>
<% if current_user.has_role? :admin %>

          <%= link_to 'ORDER FOR THIS CUSTOMER', orderas_account_path(account), class: 'btn btn-info' %>
<p>
  <strong>Seller level:</strong>
  <% if @account.seller_level  && @account.seller_level != '' %>
    <%= @account.seller_level %>
  <% else %>
    <%= link_to 'Please moderate', edit_account_path(@account), class: 'btn btn-warning' %>
  <% end %>
</p>
<% end %>
<% if (!current_user.account.approved?) || (current_user.has_role? :admin) %>
<%= link_to 'Edit account details', edit_account_path(@account), class: 'btn btn-default' %>
<% end %>
</div>
<div class="col-md-6">
<%= render 'orders/index' %>
</div>
</div>