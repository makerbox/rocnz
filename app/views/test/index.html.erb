<%= @results %><br>
LAST DISCOUNT ADDED:<%= Discount.all.last.created_at %><br>
LAST PRODUCT ADDED:<%= Product.all.last.created_at %><hr>
<%= current_user.mimic.account.code %>|
<%= current_user.mimic.account.discount %>|<br>
<hr>
account.discount<br>
<% Account.all.each do |a| %>
<%= a.discount %>
<% end %>
<hr>
<%= Discount.all.where(product: ('prod_group' || 'prod_code' || 'PM'), customer: (current_user.mimic.account.code.strip || current_user.mimic.account.discount.strip)).first %>
<% Discount.all.each do |d| %>
<%= d.customer %> -- <%= d.discount %> -- <%= d.disctype %> -- <%= d.product %> -- <%= d.maxqty %><br>
<% end %>



<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>
<%  if Contact.find_by(code:'running', email:'running') %>
	RUNNING SEED
	<% else %>
	SEED COMPLETE
	<% end %>