[<%= Account.all.count %> vs <%= User.all.count %>]
<% @output.each do |o| %>
	<b><%= 0.email %></b> ....... <%= o.created_at %><br>
<% end %>
<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>