[<%= Account.all.count %> vs <%= User.all.count %>]<br>




<% @discounts.each do |d| %>
 <%= d.customer %>
 <%= d.maxqty %>
 <%= d.disctype %>
<% end %>
<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>