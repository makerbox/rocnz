LAST DISCOUNT UPLOADED AT <%= @lastdiscount.created_at %>
<hr>
CODE | GROUP | CATEGORY
<% @products.each do |p| %>
<%= p.code %> | <%= p.group %> | <%= p.category %>
<br>
<% end %>


<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>
<%  if Contact.find_by(code:'running', email:'running') %>
	RUNNING SEED
	<% else %>
	SEED COMPLETE
	<% end %>