<% @discounts.each do |d| %>
<%= d.product %> = <%= d.customer %> = = <%= d.discount %><br>
<% end %>
<% @products.each do |p| %>
<%= p.code %>
<% end %>
<%= Product.all.count %>952



<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>
<%  if Contact.find_by(code:'running', email:'running') %>
	RUNNING SEED
	<% else %>
	SEED COMPLETE
	<% end %>