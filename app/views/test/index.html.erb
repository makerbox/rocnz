[<%= Account.all.count %> vs <%= User.all.count %>]<br><%= current_user.mimic.account.discount %>
<% Account.all.each do |o| %>
<%= o.discount %>--
<% end %>
<% @prices.each do |price| %>
<%= price.discount %><br>
<%= price.product %>
    <hr>
<% end %>
<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>