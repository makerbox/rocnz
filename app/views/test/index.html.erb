[<%= Account.all.count %> vs <%= User.all.count %>]<br>
<%= @results %>
<% if user_signed_in? %>
<%= current_user.role %>
<% end %>
<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>
<%  if Contact.find_by(code:'running', email:'running') %>
	RUNNING SEED
	<% else %>
	SEED COMPLETE
	<% end %>