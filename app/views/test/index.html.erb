<% @results.each do |r| %>
<%= r %><br>
<% end %>
<% Product.all.each do |prod| %>
	<%= prod.calc_discount(current_user, prod.price1, prod.group, prod.code, prod.pricecat, 1) %>
<% end %>
<%= Product.all.count %>952



<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>
<%  if Contact.find_by(code:'running', email:'running') %>
	RUNNING SEED
	<% else %>
	SEED COMPLETE
	<% end %>