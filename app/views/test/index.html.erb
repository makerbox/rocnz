<% @discos.each do |d| %>
<%= d.product %>--<%= d.customer %>--<%= d.maxqty %>--<%= d.discount %><br>
<% end %>



<%= link_to 'PULL', home_pull_path, class: 'btn btn-info' %>
<%  if Contact.find_by(code:'running', email:'running') %>
	RUNNING SEED
	<% else %>
	SEED COMPLETE
	<% end %>