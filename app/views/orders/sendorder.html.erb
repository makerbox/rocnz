<%= simple_form_for(@order, url: orderout_order_path(@order), html: {method: "get"}) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">

   
notes:
    <%= f.text_field :notes %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
